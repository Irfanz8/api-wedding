# Environment Variables

Copy this file to `.env.local` or `.dev.vars` and fill in your actual values.

## Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key

## JWT Secret
# Generate a strong secret using: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-here

## API Domain
API_DOMAIN=http://localhost:8787
# For production: API_DOMAIN=https://your-api-domain.com

## Cloudflare Worker Configuration (Optional)
# Add these to your wrangler.toml for production deployment
ACCOUNT_ID=your-cloudflare-account-id
API_TOKEN=your-cloudflare-api-token
